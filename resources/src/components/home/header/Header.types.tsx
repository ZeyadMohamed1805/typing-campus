export type THeaderNavigationProps = {
    props: {
        className: string;
    };
};

export type THeaderMenuIconProps = {
    props: {
        onClick: () => void;
    };
};

export type THeaderOverlayProps = {
    props: {
        condition: boolean;
        onClick: () => void;
    };
};
